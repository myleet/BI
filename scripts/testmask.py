#!/usr/bin/env python
from __future__ import print_function
from math import ceil
img = binearize(get_im(fname), threshold)
if surface_dilation_ini!=0.0:
	surface_dilation  = abs(ceil(surface_dilation_ini))
	surface_dilation2 = surface_dilation*surface_dilation